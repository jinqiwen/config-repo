spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jinqiwen/config-repo
          username: jinqiwen
          password: 15256221809jin
eureka:
  client:
    service-url:
       defaultZone: http://localhost:8761/eureka/
management:
  endpoints:
    web:
      expose: "*"
